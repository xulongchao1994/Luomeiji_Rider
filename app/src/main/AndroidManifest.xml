<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.luomeiji_rider">

    <application
        android:name=".RiderAppliction"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--订单记录-->
        <activity android:name=".ui.orderlist.OrderListActivity"></activity>
        <!--收支明细-->
        <activity android:name=".ui.getmoeny.GetMoenyActivity"></activity>
        <!--用户评价-->
        <activity android:name=".ui.usercomment.UserCommentActivity"></activity>
        <!--我的钱包-->
        <activity android:name=".ui.mywallet.MyWalletActivity"></activity>
        <!--登陆-->
        <activity android:name=".ui.login.LoginActivity"></activity>
        <!--注册-->
        <activity android:name=".ui.signup.SignupActivity"></activity>
        <!--修改密码-->
        <activity android:name=".UI.password.SetPswActivity"></activity>
    </application>

</manifest>